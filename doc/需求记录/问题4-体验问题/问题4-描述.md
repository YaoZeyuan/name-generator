# 问题描述

当前存在以下问题, 请阅读文档，看提供的信息是否足够进行修改。若足够，请再联系我确认后开工。若不足，请用@yao 在文档中进行标记，我会进行回复

1.  财富论的查询来源中，应为原始公司名，或者说包含这个分词的基金或者公司名(可以用基金:xxx , 公司:xxx)区分。目前只有2个字，不符合要求
2.  点击问号，查看姓名库来源时，关闭按钮横向挤满了整个对话框，导致来源名被挤成竖排的了，不不符合要求
    1.  `具体来源见项目 README.md`是我想告诉你的信息，不用展示出来……
3.  首次是生成候选，再点可以继续生成下一批。可以加个按钮分页，继续生成后进入下一页进行展示
4.  默认姓氏初始为`张`
5.  页面中输入的参数，默认记录到LocalStorage中，下次进入时，优先使用LocalStorage中的配置----使用json存储，方便排查
6.  `不指定字，随便看看`按钮横向缩减为原来的一半，左边加一个新按钮：重置。重置后清空LocalStorage并刷新页面，以还原到最初的状态
7.  支持收藏，收藏后存储在LocalStorage中，名字标签上加一个黄色的五角星⭐️，以和普通名字进行区分
8.  `从财富论中生成候选`按钮横向缩减为原来的1/3, 左边加一个新按钮： 收藏，点击后展示已收藏的名字。中间加一个 导出csv，点击后将当前正在展示的名字(无论是收藏的名字还是生成的名字)列表导出成csv文件。如果空间不够， `从财富论中生成候选`按钮就换一行展示