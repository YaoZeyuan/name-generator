# 多音字取音标准

## 说明

这篇文档用于定义一些多音字在`诗云`中约定的读音. 按照默认规则, 姓名中禁止出现多音字, 但在实践操作中, 有类似`中(zhōng/zhòng)`, `华(huá/huà)`的字, 虽然多音, 但在姓名中一般作为单音字使用, 不应被排除在外.

具体原则:

对已收集的 100 万+候选名进行解析, 找到其中涉及的多音字, 以及对应场景(见:[第一版: 生成二字候选名时统计发现的多音字及出现场景](./database/char_db/第一轮统计发现的多音字/生成二字候选名时统计发现的多音字及出现场景.md), 共 435 个字符)


#   